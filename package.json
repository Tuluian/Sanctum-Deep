{
  "name": "sanctum-ruins",
  "version": "0.1.0",
  "description": "Browser-based roguelike deck-builder",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "node server.js",
    "heroku-postbuild": "npm run build",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "express": "^4.22.1"
  },
  "devDependencies": {
    "jsdom": "^27.2.0",
    "typescript": "^5.3.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.6"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
